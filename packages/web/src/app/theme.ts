import { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
  borderRadius: '10px',
  colors: {
    warning: '#FFCA58',
  },
  button: {
    primary: {
      bg: '#0A45C2',
    },
    borderRadius: '10px',
  },
};

export { theme };
